# Netlify: publish root, production branch "develop",
# production domain: queueless.co.ke (DNS at Truehost)
# staging/alias: queueless.thewolfgang.tech

[build]
  publish = "."
  command = "echo 'Static site — no build'"
